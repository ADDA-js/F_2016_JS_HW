console.log("word.js is working");

function Word() {

	this.letters = [' '];

	this.getLetters = function(newWord){
		newWord.split; 

	}
	
	this.isFound = function(hidden){
		if (this.hidden === false)
			this.isFound = true; 

		}else{
			
			this.isFound = false; 
		}


	this.try = function (letter) 





		} 

	

	// return newWord + Letter objects for every letter in newWord 
	 
	 var newWord = new getLetters(); 



	// var newWord = {
	// 	letter: [] 

		// newWord

		// ###letter object for every letter in new word 
		// so each letter entered into the prompt newWord should be turned into an object















